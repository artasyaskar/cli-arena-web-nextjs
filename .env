# ✅ Prisma + SQLite (Default local DB for development)
DATABASE_URL="file:./dev.db"

# ✅ PostgreSQL credentials (Optional: if switching to Postgres later)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=cli_arena_db

# ✅ PGAdmin config (Optional GUI DB access)
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin

# ✅ Local Redis (Used if using ioredis directly)
REDIS_URL=redis://localhost:6379

# ✅ Upstash Redis (Used with @upstash/ratelimit)
# ⚠️ Only one set of Upstash credentials should be defined
UPSTASH_REDIS_REST_URL=https://your-upstash-id.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-secret-upstash-token

# .env.test
UPSTASH_REDIS_REST_URL=https://dummy.upstash.io
UPSTASH_REDIS_REST_TOKEN=dummy-token-123
